@model  FirmeIDS.Models.Firma
@{
    ViewBag.Title = "NewItem";
}

@using (Html.BeginForm("Register", "Firme", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h3>Add A New Item</h3>

    <div class="form-group">
        @Html.HiddenFor(m => m.Id, new { @class = "form-control" })
        <div class="col-md-10">
            @Html.HiddenFor(m => m.Id, new { @class = "form-control" })
        </div>
    </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Nume, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.Nume, new { @class = "form-control" })
            </div>
        </div>

        




        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" class="btn btn-primary btn-lg" value="Register" />
                
            </div>
        </div>


}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
